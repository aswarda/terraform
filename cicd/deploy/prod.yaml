# PROD manifest — namespace prod. Deployed ONLY after you APPROVE the promotion.
# Same image as dev (the exact one built by CI); Cloud Deploy swaps "sampleapp"
# for the real Artifact Registry image at release time (--images=sampleapp=...).
apiVersion: v1
kind: Namespace
metadata:
  name: prod
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sampleapp
  namespace: prod
  labels:
    app: sampleapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sampleapp
  template:
    metadata:
      labels:
        app: sampleapp
    spec:
      containers:
        - name: web1
          image: sampleapp
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "50m"
              memory: 64Mi
            limits:
              cpu: "250m"
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: sampleapp
  namespace: prod
spec:
  type: NodePort
  selector:
    app: sampleapp
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sampleapp
  namespace: prod
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: sampleapp
                port:
                  number: 80
